// document.getElementById('something')
// $('#something')
// This two will show the same result. 

// const { table } = require("console");
// const { eventNames, connected } = require("process");

// most cases 
/*
console log or software engineering log levels are Fatal, error, warn, info , debug, trace, all, off 
console.clear, console.info
console.warn, console.error, console.count
*/

/*
in browser console: monitor(functionName)[this will monitor that which function is called and when called]
unmonitor(functionName)
*/
// monitorEvents(eventNames);
// unmonitorEvents(eventNames);
// monitorEvents('click', 'mouse')

// some documentation for [chrome devtools console api]

// breakpoint in source tab, snippet, workspace, scope,watch, callstack, mouse click immidiate break point particular line in code .